{
  "name": "@omnihilbras/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @omnihilbras/sdk build && pnpm build && node --test test/*.test.js"
  },
  "dependencies": {
    "@omnihilbras/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.9.0"
  }
}
